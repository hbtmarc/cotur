/** view: home.js */
export function renderHome() {
  return `
    <div class="view-header">
      <h1>Início <span class="placeholder-badge">placeholder</span></h1>
      <p>Bem-vindo ao Cotur. Selecione uma seção no menu acima.</p>
    </div>
    <div class="card">
      <p style="color:var(--color-muted);font-size:.875rem">
        Conteúdo da tela inicial será implementado na Etapa 2.
      </p>
    </div>`;
}
