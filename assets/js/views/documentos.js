/** view: documentos.js */
export function renderDocumentos() {
  return `
    <div class="view-header">
      <h1>Documentos <span class="placeholder-badge">placeholder</span></h1>
      <p>Lista de documentos disponíveis.</p>
    </div>
    <div class="card">
      <p style="color:var(--color-muted);font-size:.875rem">
        Listagem e CRUD de documentos serão implementados na Etapa 2.
      </p>
    </div>`;
}
